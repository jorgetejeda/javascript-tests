// Exercise:
// Create a function which it takes a senteces and return each word in capital case
// Read the test to see what the expected output is.
function capitalWords(text) {
    // code
}

// Test your code while you're at it!
// write in the console: node ./exercises/capitalWords.js
function print(sentence) {
    console.log(capitalWords(sentence))
}

const sentence = 'The quick brown Fox jumps over the little lazy Dog'
print(sentence)

module.exports = capitalWords
