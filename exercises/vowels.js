// Exercise:
// Create a function which it takes a word or sentence and return how many vowels it has
// Read the test to see what the expected output is.
function vowels(wordOrSentence) {
    // code
}

// Test your code while you're at it!
// write in the console: node ./exercises/vowels.js
function print(sentence) {
    console.log(vowels(sentence))
}

const sentence = 'anita lava la tina'
print(sentence)

module.exports = vowels