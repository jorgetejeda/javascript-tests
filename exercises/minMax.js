// Exercise:
// Create a function which it takes an array of numbers and return the min and max number
// Try to not use Math.min() and Math.max() functions
// Read the test to see what the expected output is.
function getMinMax(arr){
    // code
}
// Test your code while you're at it!
// write in the console: node ./exercises/getMinMax.js
function print(arr) {
    console.log(getMinMax(arr))
}

const arr = [1, 5, 3, 6, 2, 20]
print(arr)

module.exports = getMinMax